import Subtitle from "../micro/Subtitle.micro";

export default function NavComponent() {
    return (
        <nav>
            <Subtitle>
                Digital Image Processing
            </Subtitle>
        </nav>
    );
}